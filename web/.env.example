# Backend URL (server-side only, used by Next.js rewrites to proxy API calls)
BACKEND_URL=http://localhost:8080

# Backend auth URL (client-side, used for the Google OAuth login redirect)
NEXT_PUBLIC_AUTH_URL=http://localhost:8080
